apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "restapi.fullname" . }}-test-connection"
  labels:
{{ include "restapi.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "restapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
